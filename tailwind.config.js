/** @type {import('tailwindcss').Config} */
export default {
  darkMode: 'selector', // Use 'selector' strategy for manual toggling
    content: ["./src/**/*.{html,js}"],
    theme: {
      extend: {},
    },
    plugins: [],
  }